void make_event()
{
  /*
  auto run = new KBRun();
  run -> SetRunID(5);
  run -> SetTag("raw");
  run -> AddParameterFile("NeutronWall.par");
  run -> AddDetector(new NWDetector());

  auto channel = new NWSetChannels();
  channel -> AddData("/Users/ejungwoo/data/nscl/Cosmic_Jan_5_event.root");

  run -> Add(channel);
  run -> Init();
  */
}
